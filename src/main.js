import App from './App.svelte';
import 'bootstrap';
import 'bootstrap/dist/css/bootstrap.css';

const app = new App({
	target: document.body,
	// props: {
	// 	name: 'serverless'
	// }
});

export default app;